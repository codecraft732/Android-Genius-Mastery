package com.example.firebaseretriveaccessdata

data class User(var name: String? = null, var age: String? = null){

    // No need for explicit getters and setters in Kotlin,
    // as they are automatically generated for properties.
}